<div class="video-modal" id="video-modal" hidden>
  <div class="video-modal__backdrop" data-close></div>
  <div class="video-modal__frame">
    <button class="video-modal__close" data-close type="button" aria-label="Close video">✕</button>
    <video id="video-modal-player" controls playsinline></video>
  </div>
</div>

<script>
  import '../scripts/video-modal.island.ts';
</script>

<style>
  .video-modal { position: fixed; inset: 0; z-index: 100; display: flex; align-items: center; justify-content: center; padding: 28px; }
  .video-modal[hidden] { display: none; }
  .video-modal__backdrop { position: absolute; inset: 0; background: rgba(0, 0, 0, 0.85); }
  .video-modal__frame { position: relative; width: min(920px, 100%); border: var(--border-w) solid var(--line); background: #000; }
  .video-modal__close {
    position: absolute; top: -52px; right: 0; z-index: 1;
    width: var(--tap); height: var(--tap); font-size: 15px; cursor: pointer;
    background: transparent; border: var(--border-w) solid var(--line); color: var(--text);
    transition: background 150ms ease, color 150ms ease, border-color 150ms ease;
  }
  .video-modal__close:hover { background: var(--amber); border-color: var(--amber); color: #000; }
  #video-modal-player { display: block; width: 100%; max-height: 80vh; }
</style>
